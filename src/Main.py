'''
    Inports
'''
import os
import datetime
import Preprocessing
from Util import Constants, Action

if(__name__ == "__main__"):
    # Action.extract(True)
    # pre = Preprocess()
    # pre.perform()
    # Preprocessing.perform()
    temp: list[str] = []
    temp.append('test')
    print(temp)
    pass